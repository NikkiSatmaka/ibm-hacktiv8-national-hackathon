# Bob configuration

- `agent-instructions.md` tells Bob to delegate complete meeting notes to the Langflow tool and report its result.
- `mcp-config.example.json` documents the connection shape without credentials.

Do not paste the Langflow component prompt here. See `../prompts/README.md` for the distinction.
