# Meeting Action Assistant — Langflow

- `Meeting Action Assistant - Template.json`: participant starting point.
- `Meeting Action Assistant - Complete.json`: completed reference.

Import one version unless you intentionally rename it. Recheck updated exports for credentials.

The agent component system prompt is documented at `../prompts/meeting-action-assistant-component.md`. It belongs inside Langflow, not in Bob's agent instructions. When updating the flow, keep the exported component prompt and the documented prompt synchronized.
