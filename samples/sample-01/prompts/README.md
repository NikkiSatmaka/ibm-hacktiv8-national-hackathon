# Langflow component prompts

`meeting-action-assistant-component.md` is the system prompt for the Meeting Action Assistant component inside Langflow. It owns meeting extraction, document formatting, conditional email, and conditional calendar behavior.

This is distinct from `../bob/agent-instructions.md`, which only tells Bob how to delegate to the Langflow tool.
